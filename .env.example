# Servidor
PORT=3000
NODE_ENV=development
API_URL=http://localhost:3000

# JWT
JWT_SECRET=altere_este_segredo_em_producao

# CORS (separe múltiplas origens por vírgula)
CORS_ORIGIN=*

# MySQL (SQL)
DB_HOST=mysql
DB_NAME=atividade
DB_USER=root
DB_PASSWORD=root

# MongoDB (NoSQL)
MONGO_URI=mongodb://mongo:27017/atividade
